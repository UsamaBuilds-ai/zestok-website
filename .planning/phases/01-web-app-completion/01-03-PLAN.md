---
phase: 01-web-app-completion
plan: 03
type: execute
wave: 3
depends_on: [01-01]
files_modified:
  - package.json
  - src/main.js
  - src/preload.js
  - src/index.html
  - src/styles.css
  - src/renderer.js
autonomous: true
requirements: [DKT-04, DKT-06]
user_setup: []

must_haves:
  truths:
    - "Footer displays the local IP address of the desktop machine"
    - "Footer has a QR code button"
    - "Clicking the QR code button opens a modal showing the server IP as a QR code"
    - "QR code encodes 'http://<local-ip>:3000' for mobile to scan and auto-configure"
    - "QR modal has a close button and can be dismissed"
  artifacts:
    - path: "package.json"
      provides: "qrcode dependency for generating QR code data URLs"
      contains: "\"qrcode\": \"^1.5.4\""
    - path: "src/main.js"
      provides: "system:local-ip and system:generate-qr IPC handlers"
      exports: ["IPC handlers: system:local-ip, system:generate-qr"]
    - path: "src/preload.js"
      provides: "IPC bridge exposing getLocalIp and generateQrCode"
      exports: ["getLocalIp", "generateQrCode"]
    - path: "src/index.html"
      provides: "Local IP display element, QR button, QR modal overlay"
      contains: "localIpDisplay, qrCodeBtn, qrModal, qrCodeImage"
    - path: "src/styles.css"
      provides: "Styles for footer IP display, QR button, QR modal overlay and card"
      contains: ".qr-btn, .qr-modal-overlay, .qr-modal-card, .qr-modal-close"
    - path: "src/renderer.js"
      provides: "Fetches local IP on init, handles QR button click and modal show/hide"
      contains: "window.stockApi.getLocalIp(), window.stockApi.generateQrCode()"
  key_links:
    - from: "src/renderer.js init()"
      to: "src/preload.js getLocalIp()"
      via: "window.stockApi.getLocalIp() → ipcRenderer.invoke('system:local-ip')"
      pattern: "stockApi\\.getLocalIp"
    - from: "src/renderer.js QR button click"
      to: "src/preload.js generateQrCode()"
      via: "window.stockApi.generateQrCode(url) → ipcRenderer.invoke('system:generate-qr')"
      pattern: "stockApi\\.generateQrCode"
    - from: "src/preload.js"
      to: "src/main.js"
      via: "IPC invoke 'system:local-ip' and 'system:generate-qr'"
      pattern: "system:(local-ip|generate-qr)"
    - from: "src/main.js system:local-ip"
      to: "Node.js os module"
      via: "os.networkInterfaces() to find non-internal IPv4 address"
      pattern: "os\\.networkInterfaces"
    - from: "src/main.js system:generate-qr"
      to: "qrcode package"
      via: "QRCode.toDataURL(text) to generate PNG data URL"
      pattern: "QRCode\\.toDataURL"
---

<objective>
**Phase 1, Plan 3 of 3: Footer IP display + QR code generation**

**Purpose:** Show the desktop's local IP address in the app footer so the user knows what address the mobile app should connect to. Provide a QR code button that generates a scannable QR code encoding the full server URL (`http://<ip>:3000`). The mobile app (already built with a barcode-scanner plugin) scans this QR to auto-configure its server IP — zero manual setup.

**Output:**
- `package.json` — `qrcode` dependency installed
- `src/main.js` — `system:local-ip` (detects local IPv4) and `system:generate-qr` (generates QR data URL) IPC handlers
- `src/preload.js` — exposes `getLocalIp()` and `generateQrCode()`
- `src/index.html` — footer restructured with IP display and QR button; QR modal overlay added
- `src/styles.css` — styles for IP text, QR button, QR modal card, overlay, close button
- `src/renderer.js` — fetches IP on init, handles QR button click, shows/hides modal

**File conflict note:** This plan modifies main.js, preload.js, and renderer.js which were also modified in Plan 01-02. Wave 3 ensures Plan 01-02 commits first.
</objective>

<execution_context>
@C:/Users/memon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/memon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Plan 01-01 added server status to footer — this plan extends the same footer area with IP and QR
@.planning/phases/01-web-app-completion/01-01-PLAN.md

# Plan 01-02 added stock:load/stock:save IPC handlers to main.js and preload.js — this plan adds new handlers alongside them
@.planning/phases/01-web-app-completion/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install qrcode package, add local IP detection IPC handler and QR code generation IPC handler, extend preload bridge</name>
  <read_first>src/main.js (full file), src/preload.js (16 lines)</read_first>
  <files>package.json, src/main.js, src/preload.js</files>
  <action>
    **Install the `qrcode` npm package:**
    ```
    npm install qrcode
    ```

    **In `src/main.js` — Add imports at the top of the file, right after the existing `const fs = require("fs/promises");`:**
    ```
    const os = require("os");
    ```
    (Note: `os` is a Node.js built-in, no npm install needed.)

    **In `src/main.js` — ADD two new IPC handlers. Insert them before the `app.on("before-quit"` section but can go anywhere in the IPC handler block (after existing handlers).**

    Handler 1 — `system:local-ip`:
    ```
    ipcMain.handle("system:local-ip", async () => {
      try {
        const interfaces = os.networkInterfaces();
        for (const name of Object.keys(interfaces)) {
          const ifaces = interfaces[name];
          if (!ifaces) continue;
          for (const iface of ifaces) {
            if (iface.family === 'IPv4' && !iface.internal) {
              return { ip: iface.address };
            }
          }
        }
      } catch (err) {
        console.error('Failed to detect local IP:', err.message);
      }
      return { ip: '127.0.0.1' };
    });
    ```

    Handler 2 — `system:generate-qr` (requires `qrcode` package):
    ```
    ipcMain.handle("system:generate-qr", async (_event, text) => {
      try {
        const QRCode = require('qrcode');
        const dataUrl = await QRCode.toDataURL(text, {
          width: 300,
          margin: 2,
          color: { dark: '#102033', light: '#ffffff' }
        });
        return { qrDataUrl: dataUrl };
      } catch (err) {
        console.error('QR generation failed:', err.message);
        return { qrDataUrl: null, error: err.message };
      }
    });
    ```

    **In `src/preload.js` — ADD two new methods to the stockApi object. Insert after `save:` and before `getVersion:`:**
    ```
    getLocalIp: () => ipcRenderer.invoke("system:local-ip"),
    generateQrCode: (text) => ipcRenderer.invoke("system:generate-qr", text),
    ```
  </action>
  <verify>
    <automated>
      node -e "
        const pkg = require('./package.json');
        console.assert(pkg.dependencies && pkg.dependencies.qrcode, 'qrcode package installed');

        const mainSrc = require('fs').readFileSync('./src/main.js','utf8');
        console.assert(mainSrc.includes('system:local-ip'), 'main.js has system:local-ip handler');
        console.assert(mainSrc.includes('system:generate-qr'), 'main.js has system:generate-qr handler');
        console.assert(mainSrc.includes('os.networkInterfaces()'), 'main.js uses os.networkInterfaces');
        console.assert(mainSrc.includes('QRCode.toDataURL'), 'main.js uses qrcode package');

        const preloadSrc = require('fs').readFileSync('./src/preload.js','utf8');
        console.assert(preloadSrc.includes('system:local-ip'), 'preload invokes system:local-ip');
        console.assert(preloadSrc.includes('system:generate-qr'), 'preload invokes system:generate-qr');
        console.assert(preloadSrc.includes('getLocalIp'), 'preload exposes getLocalIp');
        console.assert(preloadSrc.includes('generateQrCode'), 'preload exposes generateQrCode');

        // Test that qrcode is importable
        const QRCode = require('qrcode');
        console.assert(typeof QRCode.toDataURL === 'function', 'qrcode.toDataURL is a function');

        console.log('ALL CHECKS PASSED');

        // Verify IP detection works
        const os = require('os');
        const interfaces = os.networkInterfaces();
        let found = false;
        for (const name of Object.keys(interfaces)) {
          const ifaces = interfaces[name];
          if (!ifaces) continue;
          for (const iface of ifaces) {
            if (iface.family === 'IPv4' && !iface.internal) {
              console.log('Detected local IP:', iface.address);
              found = true;
              break;
            }
          }
          if (found) break;
        }
        if (!found) console.log('No non-internal IPv4 found (expected on headless)');
      "
    </automated>
  </verify>
  <acceptance_criteria>
    1. qrcode package installed in package.json dependencies
    2. main.js has system:local-ip IPC handler that uses os.networkInterfaces() to find the first non-internal IPv4 address
    3. main.js has system:generate-qr IPC handler that uses QRCode.toDataURL() to generate a 300x300 PNG data URL with dark=#102033 and light=#ffffff colors
    4. preload.js exposes getLocalIp() (→ system:local-ip) and generateQrCode(text) (→ system:generate-qr)
    5. qrcode module is require()-able without errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add IP display and QR button to footer, QR modal overlay, renderer IP fetch and QR interaction</name>
  <read_first>src/index.html (full, 223 lines), src/styles.css (full, 554 lines), src/renderer.js (full, 348 lines)</read_first>
  <files>src/index.html, src/styles.css, src/renderer.js</files>
  <action>
    **In `src/index.html` — RESTRUCTURE the footer** to include IP display and QR button alongside the existing status indicators and branding.

    Replace the current footer block (the entire `<footer>...</footer>` element, currently lines 215–218) with an expanded version:
    ```
    <footer>
      <div class="footer-left">
        <span class="server-status" id="serverStatusIndicator">
          <span class="status-badge disconnected"></span>
          Server: <span id="serverStatusText">Checking...</span>
        </span>
        <span class="server-status" id="dbStatusIndicator">
          <span class="status-badge disconnected"></span>
          DB: <span id="dbStatusText">Checking...</span>
        </span>
      </div>
      <div class="footer-center">
        <span class="footer-ip" id="localIpDisplay">IP: <span id="localIpValue">--</span></span>
        <button class="qr-btn" id="qrCodeBtn" type="button" title="Show QR code to connect mobile">QR</button>
      </div>
      <div class="footer-right">
        <span>Build by Usama | v<span id="appVersion"></span></span>
        <span>For Any Query Contact # 0314-2958627</span>
      </div>
    </footer>
    ```

    **In `src/index.html` — ADD the QR modal overlay** near the bottom of the file, just before the `<script>` tag (before line 221):
    ```
    <div class="qr-modal-overlay" id="qrModal" style="display:none">
      <div class="qr-modal-card">
        <button class="qr-modal-close" id="qrModalClose" type="button">&times;</button>
        <h3>Scan to Connect</h3>
        <p>Point your mobile camera at the QR code</p>
        <div class="qr-code-wrapper">
          <img id="qrCodeImage" src="" alt="QR Code" />
        </div>
        <p class="qr-server-ip" id="qrServerIp"></p>
      </div>
    </div>
    ```

    **In `src/styles.css` — REPLACE the existing footer block** (currently lines 537–543) with the expanded multi-section footer:
    ```
    footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 20px;
      color: rgba(255, 255, 255, 0.84);
      font-weight: 700;
    }

    .footer-left {
      display: flex;
      gap: 16px;
      align-items: center;
    }

    .server-status {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      font-size: 12px;
      color: rgba(255, 255, 255, 0.78);
    }

    .status-badge {
      display: inline-block;
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: #888;
      transition: background 0.3s;
    }

    .status-badge.connected {
      background: #0b8f66;
      box-shadow: 0 0 6px rgba(11, 143, 102, 0.6);
    }

    .status-badge.disconnected {
      background: #c44242;
      box-shadow: 0 0 6px rgba(196, 66, 66, 0.6);
    }

    .footer-center {
      display: flex;
      gap: 12px;
      align-items: center;
    }

    .footer-ip {
      font-size: 12px;
      color: rgba(255, 255, 255, 0.78);
    }

    .qr-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 32px;
      height: 24px;
      border: 1px solid rgba(255, 255, 255, 0.3);
      border-radius: 4px;
      background: rgba(255, 255, 255, 0.12);
      color: #fff;
      font-size: 11px;
      font-weight: 800;
      cursor: pointer;
      transition: background 0.2s;
    }

    .qr-btn:hover {
      background: rgba(255, 255, 255, 0.22);
    }

    .footer-right {
      display: flex;
      gap: 20px;
    }
    ```

    **In `src/styles.css` — ADD QR modal styles** at the end of the file (after the last rule):
    ```
    .qr-modal-overlay {
      position: fixed;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(3, 18, 45, 0.7);
      z-index: 2000;
    }

    .qr-modal-card {
      position: relative;
      width: 360px;
      padding: 32px 28px 28px;
      border-radius: 12px;
      background: #fff;
      box-shadow: 0 22px 50px rgba(3, 18, 45, 0.28);
      text-align: center;
    }

    .qr-modal-card h3 {
      margin: 0 0 4px;
      font-size: 20px;
      color: #102033;
    }

    .qr-modal-card p {
      margin: 4px 0 16px;
      font-size: 13px;
      color: #66758a;
    }

    .qr-modal-close {
      position: absolute;
      top: 12px;
      right: 16px;
      border: none;
      background: none;
      font-size: 24px;
      color: #66758a;
      cursor: pointer;
      line-height: 1;
    }

    .qr-modal-close:hover {
      color: #102033;
    }

    .qr-code-wrapper {
      display: flex;
      justify-content: center;
      margin: 12px 0;
    }

    .qr-code-wrapper img {
      width: 260px;
      height: 260px;
      border: 1px solid #d9e4f2;
      border-radius: 8px;
    }

    .qr-server-ip {
      margin: 8px 0 0;
      font-size: 14px;
      font-weight: 700;
      color: #0d65d9;
    }
    ```

    **In `src/renderer.js` — ADD IP fetch to the `init` function** (after `pollServerStatus();` and before the closing brace of init):
    ```
    // Fetch and display local IP
    window.stockApi.getLocalIp().then(({ ip }) => {
      qs("#localIpValue").textContent = ip;
    });
    ```

    **In `src/renderer.js` — ADD QR modal handler functions** near the top or anywhere after the helper functions (e.g., after `escapeHtml`):
    ```
    const showQrModal = async () => {
      const ipEl = qs("#localIpValue");
      const ip = ipEl ? ipEl.textContent : '127.0.0.1';
      const serverUrl = `http://${ip}:3000`;
      try {
        const { qrDataUrl } = await window.stockApi.generateQrCode(serverUrl);
        if (qrDataUrl) {
          qs("#qrCodeImage").src = qrDataUrl;
          qs("#qrServerIp").textContent = serverUrl;
          qs("#qrModal").style.display = 'flex';
        }
      } catch (e) {
        console.error('QR generation failed:', e);
      }
    };

    const hideQrModal = () => {
      qs("#qrModal").style.display = 'none';
    };
    ```

    **In `src/renderer.js` — ADD event listeners** inside the `bindEvents` function (after existing listeners, before the closing brace):
    ```
    qs("#qrCodeBtn").addEventListener("click", showQrModal);
    qs("#qrModalClose").addEventListener("click", hideQrModal);
    qs("#qrModal").addEventListener("click", (event) => {
      if (event.target === qs("#qrModal")) hideQrModal();
    });
    document.addEventListener("keydown", (event) => {
      if (event.key === "Escape") hideQrModal();
    });
    ```
  </action>
  <verify>
    <automated>
      node -e "
        const html = require('fs').readFileSync('./src/index.html','utf8');
        console.assert(html.includes('localIpDisplay'), 'HTML has IP display');
        console.assert(html.includes('qrCodeBtn'), 'HTML has QR button');
        console.assert(html.includes('qrModal'), 'HTML has QR modal');
        console.assert(html.includes('qrCodeImage'), 'HTML has QR image');

        const css = require('fs').readFileSync('./src/styles.css','utf8');
        console.assert(css.includes('.qr-modal-overlay'), 'CSS has QR overlay');
        console.assert(css.includes('.qr-modal-card'), 'CSS has QR card');
        console.assert(css.includes('.qr-btn'), 'CSS has QR button style');
        console.assert(css.includes('.qr-modal-close'), 'CSS has QR close button');
        console.assert(css.includes('.footer-ip'), 'CSS has IP style');
        console.assert(css.includes('.footer-center'), 'CSS has footer center');
        console.assert(css.includes('.qr-code-wrapper'), 'CSS has QR wrapper');
        console.assert(css.includes('.qr-server-ip'), 'CSS has QR server IP');

        const renderer = require('fs').readFileSync('./src/renderer.js','utf8');
        console.assert(renderer.includes('showQrModal'), 'renderer has showQrModal');
        console.assert(renderer.includes('hideQrModal'), 'renderer has hideQrModal');
        console.assert(renderer.includes('stockApi.getLocalIp'), 'renderer fetches local IP');
        console.assert(renderer.includes('stockApi.generateQrCode'), 'renderer generates QR');
        console.assert(renderer.includes('localIpValue'), 'renderer updates IP element');
        console.assert(renderer.includes('qrCodeBtn'), 'renderer binds QR button');

        console.log('ALL CHECKS PASSED');
      "
    </automated>
  </verify>
  <acceptance_criteria>
    1. Footer shows local IP address (e.g., "IP: 192.168.1.5") via #localIpValue element
    2. Footer has a QR button (#qrCodeBtn) styled with .qr-btn class
    3. Clicking "QR" button opens a modal overlay (#qrModal) with the QR code image
    4. QR code encodes the URL "http://{local-ip}:3000" for mobile to scan
    5. Modal shows the server IP text (#qrServerIp) below the QR code
    6. Modal can be closed via X button (#qrModalClose), clicking outside the card, or pressing Escape
    7. QR code is generated via `qrcode` package's `toDataURL()` as a PNG data URL (300x300)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → preload → main (IPC) | system:local-ip and system:generate-qr IPC channels — same isolation as existing channels |
| main.js → os.networkInterfaces() | Reads local network configuration — no external access |
| main.js → qrcode npm package | Local QR code generation — no network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | Local IP displayed in footer | accept | Local network IP is visible on desktop UI only — same information available via ipconfig |
| T-01-03-02 | Spoofing | QR code encodes a URL | accept | QR encodes local network URL (`http://<ip>:3000`). An attacker with physical access could scan it, but they would already need to be on the same local network. This is the intended workflow. |
| T-01-03-03 | Tampering | qrcode npm package | mitigate | Package installed from npm registry, widely used (10M+ weekly downloads, 10+ years). Covered by standard npm supply chain trust. |
| T-01-03-SC | Tampering | qrcode npm install | mitigate | qrcode is [ASSUMED] — well-established package, no suspicious patterns, pure JS (no native compilation). No blocking checkpoint needed. |
</threat_model>

<verification>
## Phase 1 Plan 3 Verification

1. `npm ls qrcode` — confirms package installed
2. Automated checks above confirm all HTML/CSS/JS elements exist
3. Manual: Launch Electron app, verify IP shows in footer
4. Manual: Click QR button, verify modal opens with QR code
5. Manual: Scan QR code with mobile device — should show URL: `http://<ip>:3000`
6. Manual: Close modal via X, click outside, Escape key
7. All existing tests pass: `node --test tests/db/connection.test.js`
8. All existing IPC handlers continue working
</verification>

<success_criteria>
1. Local IP address displayed in footer on app startup
2. QR code button visible in footer alongside IP
3. Clicking QR button opens a centered modal with a generated QR code
4. QR code encodes `http://{local-ip}:3000` — scannable by mobile app
5. Modal closes via X button, overlay click, or Escape key
6. QR code uses dark=#102033 / light=#ffffff color scheme matching app theme
7. qrcode package properly installed and importable
</success_criteria>

## Artifacts This Phase Produces

| Symbol | Kind | File | Description |
|--------|------|------|-------------|
| `qrcode` | npm package | `package.json` | QR code generation library (toDataURL) |
| `system:local-ip` | IPC handler | `src/main.js` | Returns first non-internal IPv4 via os.networkInterfaces() |
| `system:generate-qr` | IPC handler | `src/main.js` | Returns PNG data URL via QRCode.toDataURL(text) |
| `getLocalIp()` | preload method | `src/preload.js` | Invokes system:local-ip IPC, returns `{ ip }` |
| `generateQrCode(text)` | preload method | `src/preload.js` | Invokes system:generate-qr IPC, returns `{ qrDataUrl }` |
| `showQrModal()` | function | `src/renderer.js` | Generates QR from current IP, displays modal |
| `hideQrModal()` | function | `src/renderer.js` | Hides QR modal overlay |
| `#localIpDisplay` | HTML element | `src/index.html` | Footer IP display container |
| `#qrCodeBtn` | HTML button | `src/index.html` | QR code trigger button in footer |
| `#qrModal` | HTML overlay | `src/index.html` | QR modal overlay (display:none by default) |
| `#qrCodeImage` | HTML img | `src/index.html` | QR code image inside modal |
| `.qr-modal-overlay` | CSS class | `src/styles.css` | Fixed overlay with dark background |
| `.qr-modal-card` | CSS class | `src/styles.css` | White card containing QR image |
| `.qr-btn` | CSS class | `src/styles.css` | QR button in footer |
</success_criteria>

<output>
Create `.planning/phases/01-web-app-completion/01-03-SUMMARY.md` when done
</output>
