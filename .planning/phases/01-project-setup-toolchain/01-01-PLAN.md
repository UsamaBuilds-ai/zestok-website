---
phase: 01-project-setup-toolchain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/package.json
  - mobile/.gitignore
  - mobile/vite.config.js
  - mobile/capacitor.config.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Mobile project has a dedicated package.json with all Capacitor dependencies"
    - "Capacitor config exists at mobile/capacitor.config.ts with correct app identity, server config, and plugin settings"
    - "Vite build config exists at mobile/vite.config.js for the vanilla JS app"
    - "All D-01 through D-11 decisions are reflected in the configuration files"
  artifacts:
    - path: "mobile/package.json"
      provides: "Mobile app dependency manifest"
      contains: "@capacitor/core, @capacitor/android, @capacitor/preferences, @capacitor/app as dependencies; @capacitor/cli as devDependency; vite as devDependency"
    - path: "mobile/capacitor.config.ts"
      provides: "Capacitor configuration with app identity, server, and plugin settings"
      contains: "appId, appName, webDir, server.url, server.cleartext, SplashScreen, StatusBar, CapacitorHttp plugins"
    - path: "mobile/vite.config.js"
      provides: "Vite bundler configuration for mobile app"
      contains: "root, build.outDir, server.port"
  key_links:
    - from: "mobile/capacitor.config.ts"
      to: "mobile/package.json"
      via: "Imports CapacitorConfig type from @capacitor/cli"
      pattern: "@capacitor/cli"
    - from: "mobile/vite.config.js"
      to: "mobile/package.json"
      via: "Imports defineConfig from vite"
      pattern: "from 'vite'"
---

<objective>
Create the mobile app project scaffold with dedicated package.json, install all missing Capacitor packages, and configure Vite + Capacitor with all Phase 1 decisions.

Purpose: Establish the mobile app directory structure and build toolchain that every subsequent phase builds on. The Walking Skeleton needs a working build pipeline before any UI code can run.

Output: mobile/ directory with package.json (deps installed), .gitignore, vite.config.js, capacitor.config.ts
</objective>

<execution_context>
@C:/Users/memon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/memon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-toolchain/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md

## Key Context

- The mobile app lives in `mobile/` subdirectory, separate from Electron `src/`
- Existing root `package.json` already has `@capacitor/android@^8.4.1` but the mobile directory needs its own complete deps
- Do NOT modify any files outside `mobile/` — leave root package.json, src/, and all Electron files untouched
- Package versions: @capacitor/core@^8.4.1, @capacitor/cli@^8.4.1, @capacitor/preferences@^8.0.0, @capacitor/app@^8.0.0, vite@^6.x
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mobile/ directory structure and dedicated package.json</name>
  <files>
    mobile/package.json
    mobile/.gitignore
  </files>
  <action>
    Create `mobile/` subdirectory at project root with the following structure:
    - `mobile/package.json` with metadata:
      - name: "stock-mgmt-mobile"
      - version: "1.0.0"
      - private: true
      - type: "module"
      - scripts: {"dev": "vite", "build": "vite build", "preview": "vite preview", "cap:init": "npx cap init", "cap:add:android": "npx cap add android", "cap:sync": "npx cap sync", "cap:run:android": "npx cap run android"}
      - dependencies: @capacitor/core ^8.4.1, @capacitor/android ^8.4.1, @capacitor/preferences ^8.0.0, @capacitor/app ^8.0.0
      - devDependencies: @capacitor/cli ^8.4.1, vite ^6.1.0
    - `mobile/.gitignore` ignoring: node_modules/, dist/, android/.gradle, android/build, android/local.properties, *.jks, *.keystore

    Per D-01, D-02: app identity will live in capacitor.config.ts, not in package.json (package.json name is the npm package name, not the app display name).

    IMPORTANT: Do NOT install @capacitor/splash-screen or @capacitor/status-bar or @capacitor/network as separate packages — these will be configured via capacitor.config.ts plugin settings but are built-in or configured through core in Capacitor 8. Only install explicitly listed deps.
  </action>
  <verify>
    <automated>
      cd mobile; if ($?) { npm install --no-audit --no-fund; if ($?) { node -e "const p = require('./package.json'); ['@capacitor/core','@capacitor/android','@capacitor/preferences','@capacitor/app'].forEach(d => { if(!p.dependencies[d]) {process.exit(1)} }); if(!p.devDependencies['@capacitor/cli']) {process.exit(1)}; if(!p.devDependencies['vite']) {process.exit(1)}; console.log('All deps verified')" } }
    </automated>
  </verify>
  <done>
    mobile/package.json exists with all 4 regular dependencies and 2 devDependencies; npm install succeeds; node_modules installed
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Vite configuration for the mobile app</name>
  <files>
    mobile/vite.config.js
  </files>
  <action>
    Create `mobile/vite.config.js` with:
    ```
    import { defineConfig } from 'vite';

    export default defineConfig({
      root: '.',
      build: {
        outDir: 'dist',
        emptyOutDir: true,
      },
      server: {
        port: 3001,
        strictPort: false,
      },
    });
    ```
    - root: '.' means Vite uses the mobile/ directory as root
    - outDir: 'dist' matches D-06 webDir setting
    - port: 3001 avoids conflict with backend port 3000
    - No React/Vue/Svelte plugins — pure vanilla JS (per RESEARCH.md recommendation, 6 thin screens don't need framework overhead)
  </action>
  <verify>
    <automated>
      Test-Path "mobile/vite.config.js"
    </automated>
  </verify>
  <done>
    mobile/vite.config.js exists with correct root, outDir, and port settings
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Capacitor configuration with all Phase 1 decisions</name>
  <files>
    mobile/capacitor.config.ts
  </files>
  <action>
    Create `mobile/capacitor.config.ts` with the following configuration, implementing ALL of the following decisions explicitly:

    App Identity (D-01, D-02, D-03):
    - appId: "com.stockmgmt.mobile"
    - appName: "Stock Management"
    - UIDisplayName: not set explicitly — inherits from appName per D-03

    Server Config (D-04, D-05):
    - server.url: "http://84.235.249.239:3000"
    - server.cleartext: true
    - server.androidScheme: "http"

    Web Directory (D-06):
    - webDir: "dist"

    Plugins (D-07 through D-11):
    - SplashScreen: launchShowDuration: 2000, backgroundColor: "#1a1a2e"
    - StatusBar: style: "DARK", backgroundColor: "#1a1a2e"
    - CapacitorHttp: enabled: true (bundled in @capacitor/core, enables native HTTP patching)

    Notes on plugins D-09 (Preferences) and D-10 (App) and D-11 (Network):
    - Preferences (@capacitor/preferences) is imported directly in JS code — no config key needed in capacitor.config.ts
    - App (@capacitor/app) is imported directly in JS code — no config key needed
    - Network (@capacitor/network) will be configured in Phase 2 — import-based, not config-based

    The capacitor.config.ts MUST:
    - Import type CapacitorConfig from '@capacitor/cli'
    - Default export the config object
    - Include all the fields above

    Per the STACK.md research: Do NOT embed android.buildOptions in this phase — those are for Phase 6 (signing).
  </action>
  <verify>
    <automated>
      node -e "
        const fs = require('fs');
        const content = fs.readFileSync('mobile/capacitor.config.ts', 'utf8');
        const checks = [
          ['appId', 'com.stockmgmt.mobile'],
          ['appName', 'Stock Management'],
          ['webDir', 'dist'],
          ['url', 'http://84.235.249.239:3000'],
          ['cleartext', 'true'],
          ['androidScheme', 'http'],
          ['SplashScreen', 'launchShowDuration'],
          ['StatusBar', 'backgroundColor'],
          ['CapacitorHttp', 'enabled'],
        ];
        checks.forEach(([key, val]) => {
          if (!content.includes(key) || (val && !content.includes(val))) {
            console.error('Missing:', key, val); process.exit(1);
          }
        });
        console.log('All D-XX decisions verified in config');
      "
    </automated>
  </verify>
  <done>
    mobile/capacitor.config.ts exists with all D-01 through D-11 decisions implemented; automated verification passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local node_modules | Untrusted third-party packages installed into mobile/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm packages (core, cli, plugins) | mitigate | npm install uses lockfile; verify package versions match ^8.4.1 exact; all packages are from Ionic team (capacitorjs.com official) |
| T-01-02 | Spoofing | npm registry mirror | mitigate | npm install fetches from registry.npmjs.org (no custom registries configured) |
</threat_model>

<verification>
1. `mobile/package.json` exists with correct dependencies
2. `mobile/node_modules/` contains all 4 Capacitor packages (core, android, preferences, app) and devDeps (cli, vite)
3. `mobile/vite.config.js` exists with correct settings
4. `mobile/capacitor.config.ts` exists with all D-01 through D-11 decisions
5. Automated config verification script passes (checks all decision keys present)
6. `node -e "require('vite')"` works from mobile/ directory
7. `npx cap --version` works from mobile/ directory (verifies CLI installed)
</verification>

<success_criteria>
- [ ] mobile/package.json created with all 6 dependencies
- [ ] npm install completed successfully in mobile/
- [ ] mobile/vite.config.js created with root='.', outDir='dist', port=3001
- [ ] mobile/capacitor.config.ts created per all D-XX decisions
- [ ] Automated checks pass for config content verification
- [ ] No files outside mobile/ were modified
</success_criteria>

<output>
Create `.planning/phases/01-project-setup-toolchain/01-01-SUMMARY.md` when done
</output>
