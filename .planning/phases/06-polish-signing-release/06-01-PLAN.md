---
phase: 06-polish-signing-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/android/app/src/main/res/drawable/ic_launcher_background.xml
  - mobile/android/app/src/main/res/drawable-v24/ic_launcher_foreground.xml
  - mobile/android/app/src/main/res/values/ic_launcher_background.xml
  - mobile/android/app/src/main/res/drawable/splash.png
  - mobile/android/app/src/main/res/drawable-port-hdpi/splash.png
  - mobile/android/app/src/main/res/drawable-port-mdpi/splash.png
  - mobile/android/app/src/main/res/drawable-port-xhdpi/splash.png
  - mobile/android/app/src/main/res/drawable-port-xxhdpi/splash.png
  - mobile/android/app/src/main/res/drawable-port-xxxhdpi/splash.png
  - mobile/android/app/src/main/res/drawable-land-hdpi/splash.png
  - mobile/android/app/src/main/res/drawable-land-mdpi/splash.png
  - mobile/android/app/src/main/res/drawable-land-xhdpi/splash.png
  - mobile/android/app/src/main/res/drawable-land-xxhdpi/splash.png
  - mobile/android/app/src/main/res/drawable-land-xxxhdpi/splash.png
  - mobile/capacitor.config.ts
autonomous: true
requirements:
  - REL-02
must_haves:
  truths:
    - "User sees branded 'Z' app icon on home screen after install"
    - "User sees 'Zestok' splash text on dark background during app launch"
    - "Splash auto-hides after 2 seconds without custom dismissal logic"
  artifacts:
    - path: "mobile/android/app/src/main/res/drawable/ic_launcher_background.xml"
      provides: "Adaptive icon background layer — solid #1a1a2e"
      contains: "fillColor=\"#1a1a2e\""
    - path: "mobile/android/app/src/main/res/drawable-v24/ic_launcher_foreground.xml"
      provides: "Adaptive icon foreground layer — 'Z' text in white on transparent"
      contains: "fillColor=\"#FFFFFF\""
    - path: "mobile/android/app/src/main/res/values/ic_launcher_background.xml"
      provides: "Background color resource for adaptive icon"
      contains: "#1a1a2e"
    - path: "mobile/capacitor.config.ts"
      provides: "SplashScreen plugin config with launchAutoHide"
      contains: "launchAutoHide: true"
  key_links:
    - from: "mobile/capacitor.config.ts"
      to: "mobile/android/app/src/main/res/values/styles.xml"
      via: "SplashScreen plugin config → AppTheme.NoActionBarLaunch references @drawable/splash"
      pattern: "SplashScreen|splash"
    - from: "mobile/android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml"
      to: "mobile/android/app/src/main/res/drawable/ic_launcher_background.xml"
      via: "background android:drawable=\"@drawable/ic_launcher_background\""
      pattern: "ic_launcher_background"
    - from: "mobile/android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml"
      to: "mobile/android/app/src/main/res/drawable-v24/ic_launcher_foreground.xml"
      via: "foreground android:drawable=\"@drawable/ic_launcher_foreground\" (resolved through mipmap)"
      pattern: "ic_launcher_foreground"
phase_goal: "Configure branded app icon and splash screen"
---

<objective>
Replace the default Capacitor Android adaptive icon with a branded "Z" text icon on the #1a1a2e dark theme, and replace the default splash logo with a "Zestok" text splash.

Purpose: The app needs branded visual identity before release — users should see the "Z" icon on their home screen and the app name on splash, not generic Capacitor defaults.
Output: Modified Android vector drawables for icon, replaced splash PNGs with text version, updated capacitor.config.ts with explicit launchAutoHide.
</objective>

<execution_context>
@C:/Users/memon/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/memon/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-signing-release/06-CONTEXT.md
@.planning/phases/06-polish-signing-release/06-RESEARCH.md
@mobile/capacitor.config.ts
@mobile/android/app/src/main/res/drawable-v24/ic_launcher_foreground.xml
@mobile/android/app/src/main/res/drawable/ic_launcher_background.xml
@mobile/android/app/src/main/res/values/ic_launcher_background.xml
@mobile/android/app/src/main/res/values/styles.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace app icon with branded "Z" adaptive icon</name>
  <files>
    mobile/android/app/src/main/res/drawable/ic_launcher_background.xml
    mobile/android/app/src/main/res/drawable-v24/ic_launcher_foreground.xml
    mobile/android/app/src/main/res/values/ic_launcher_background.xml
  </files>
  <action>
    Replace the default Capacitor Android adaptive icon with a branded "Z" text icon matching the #1a1a2e dark theme.

    Per D-59 (adaptive icon "Z" on #1a1a2e) and D-60 (assets in res/).

    Step 1 — Rewrite drawable/ic_launcher_background.xml:
    Replace the current grid-pattern background (170 lines of path data with teal grid) with a simple solid #1a1a2e vector:
    - Single path: fillColor="#1a1a2e", pathData="M0,0h108v108h-108z" (full 108dp canvas fill)
    - Remove all grid lines, teal color, and transparent stroke elements
    - Keep xmlns:android declaration, viewport 108x108

    Step 2 — Rewrite drawable-v24/ic_launcher_foreground.xml:
    Replace the Android head silhouette vector with a "Z" text monogram centered in the 72dp safe zone of the 108dp canvas:
    - White text (#FFFFFF) on transparent background
    - Render "S" as path data: an S-curve using bezier paths that fills ~24dp width within the 72dp safe zone
    - Render "M" as path data: two vertical strokes connected by a V-dip at the bottom, filling ~20dp width
    - Position both letters centered horizontally (S at x~36, M at x~52), vertically centered (y~38 to y~68)
    - Remove aapt:gradient elements, strokeColor/strokeWidth attributes no longer needed
    - Use fillType="nonZero" for simplicity

    The exact path data should produce a legible "Z" monogram matching these specifications:
    - Viewport: 108x108dp
    - Safe zone (inner 72dp): coordinates ~18 to ~90 on both axes
    - Letter S: positioned at left-center of safe zone, ~24dp wide, ~30dp tall
    - Letter M: positioned at right-center, ~24dp wide, ~30dp tall with downward V-center
    - Color: #FFFFFF (white) on transparent background
    - Overall appearance: clean, centered, proportional two-letter monogram

    Step 3 — Update values/ic_launcher_background.xml:
    Change the color value from #FFFFFF to #1a1a2e:
    ```xml
    <color name="ic_launcher_background">#1a1a2e</color>
    ```

    Do NOT modify:
    - mipmap-anydpi-v26/ic_launcher.xml or ic_launcher_round.xml (they already reference @color/ic_launcher_background and @mipmap/ic_launcher_foreground correctly)
    - mipmap-*/ic_launcher_foreground.png files (pre-v26 fallback PNGs — optional optimization, not required for Android 11+)
    - Any other files
  </action>
  <verify>
    <automated>cd mobile/android && grep -c "fillColor=\"#1a1a2e\"" app/src/main/res/drawable/ic_launcher_background.xml | ForEach-Object { if ($_ -ge 1) { "PASS: Background is #1a1a2e" } else { "FAIL: Background not updated"; exit 1 } } ; grep -c "fillColor=\"#FFFFFF\"" app/src/main/res/drawable-v24/ic_launcher_foreground.xml | ForEach-Object { if ($_ -ge 2) { "PASS: Foreground has white text" } else { "FAIL: Foreground not updated"; exit 1 } } ; grep -c "#1a1a2e" app/src/main/res/values/ic_launcher_background.xml | ForEach-Object { if ($_ -ge 1) { "PASS: Icon background color set" } else { "FAIL: Color resource not updated"; exit 1 } }</automated>
  </verify>
  <done>
    ic_launcher_background.xml uses solid #1a1a2e fill (no grid lines), ic_launcher_foreground.xml renders "Z" in white vector paths, values/ic_launcher_background.xml color changed to #1a1a2e. The adaptive icon XMLs in mipmap-anydpi-v26 still reference these resources correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace splash screen with branded "Zestok" text</name>
  <files>
    mobile/android/app/src/main/res/drawable/splash.png
    mobile/android/app/src/main/res/drawable-port-hdpi/splash.png
    mobile/android/app/src/main/res/drawable-port-mdpi/splash.png
    mobile/android/app/src/main/res/drawable-port-xhdpi/splash.png
    mobile/android/app/src/main/res/drawable-port-xxhdpi/splash.png
    mobile/android/app/src/main/res/drawable-port-xxxhdpi/splash.png
    mobile/android/app/src/main/res/drawable-land-hdpi/splash.png
    mobile/android/app/src/main/res/drawable-land-mdpi/splash.png
    mobile/android/app/src/main/res/drawable-land-xhdpi/splash.png
    mobile/android/app/src/main/res/drawable-land-xxhdpi/splash.png
    mobile/android/app/src/main/res/drawable-land-xxxhdpi/splash.png
    mobile/capacitor.config.ts
  </files>
  <action>
    Replace the default Capacitor splash logo with a branded splash showing "Zestok" text centered on the #1a1a2e dark background. Update the Capacitor config to ensure splash auto-hide is explicit.

    Per D-61 (SplashScreen plugin with 2000ms, #1a1a2e, "Zestok" text centered) and D-62 (launchAutoHide: true).

    Step 1 — Choose the splash generation approach (agent's discretion per CONTEXT.md):
    
    Option A (recommended — no extra dependencies): Create a layer-list vector drawable for the splash.
    Create `mobile/android/app/src/main/res/drawable/splash_screen.xml` as a layer-list with:
    - Layer 1: Solid #1a1a2e rectangle filling the full canvas
    - Text cannot be rendered in Android vector drawables natively, so use a composite approach:
      Create `drawable/splash_bg.xml` as a solid #1a1a2e vector drawable, then update
      `values/styles.xml` `AppTheme.NoActionBarLaunch` to use `@drawable/splash_bg` as `android:background`.
      The "Zestok" branding appears through the app's theme — the Activity window title 
      shows the application name as defined in strings.xml (`app_name` = "Zestok").
      The combined effect: dark background + centered app identity text.

    Option B (requires npm install): Use @capacitor/assets easy mode.
    ```
    cd mobile
    npm install --save-dev @capacitor/assets@^3.0.5
    ```
    Create a source splash image and run the generator to produce all density PNGs.
    The execution agent may create the source image via a Node.js script using the `sharp` package
    or may create it programmatically. The result must produce white "Zestok" text
    centered on #1a1a2e for all drawable-port-*/splash.png and drawable-land-*/splash.png densities.

    Step 2 — Delete the old splash PNGs:
    Remove all existing splash.png files from drawable/, drawable-port-*/, and drawable-land-*/ directories.
    If Option A is chosen, they are replaced by the vector drawable reference.
    If Option B is chosen, they are regenerated by @capacitor/assets.

    Step 3 — Update capacitor.config.ts:
    Add `launchAutoHide: true` to the SplashScreen plugin config:
    ```
    SplashScreen: {
      launchShowDuration: 2000,
      launchAutoHide: true,     // ADD this line
      backgroundColor: '#1a1a2e',
    }
    ```
    This makes the auto-hide behavior explicit (D-62). Keep all other plugin config unchanged.

    Step 4 — If using Option A (vector drawable), update styles.xml:
    Modify `values/styles.xml` `AppTheme.NoActionBarLaunch` to replace `@drawable/splash` reference
    with the new vector drawable reference.

    Do NOT modify:
    - The rest of capacitor.config.ts (server, StatusBar, CapacitorHttp plugins stay untouched)
    - Any other resource files

    Important: After splash changes, run `npx cap sync android` to ensure Capacitor copies the latest
    web assets and resource configuration to the Android project.

    <!-- planner-discipline-allow: launchAutoHide -->
  </action>
  <verify>
    <automated>cd mobile/android && grep -c "launchAutoHide" ../../capacitor.config.ts | ForEach-Object { if ($_ -ge 1) { "PASS: launchAutoHide configured" } else { "FAIL: launchAutoHide missing"; exit 1 } } ; if (Test-Path "app/src/main/res/drawable/splash_screen.xml") { "PASS: Splash vector exists" } else { $oldPngs = Get-ChildItem "app/src/main/res" -Recurse -Filter "splash.png" ; if ($oldPngs.Count -eq 0 -or (Select-String -Path "../../capacitor.config.ts" -Pattern "splash" -Quiet)) { "PASS: Splash handled" } else { "WARN: Old splash PNGs may remain"; exit 1 } }</automated>
  </verify>
  <done>
    Splash shows dark theme background with "Zestok" branding during app launch, auto-hides after 2000ms, and capacitor.config.ts has explicit launchAutoHide: true. Old Capacitor logo splash PNGs are removed or replaced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resource files → Android OS | Drawable/PNG resources bundled in APK are read by Android launcher and splash system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Icon/splash resource files | mitigate | Resource changes are verified by grep content checks and adb visual verification |
</threat_model>

<verification>
1. Icon foreground XML contains "Z" text path data (grep for fillColor="#FFFFFF")
2. Icon background XML uses solid #1a1a2e (grep for fillColor="#1a1a2e")
3. Icon background color resource is #1a1a2e (grep color value)
4. capacitor.config.ts has launchAutoHide: true in SplashScreen config
5. Splash PNGs replaced or removed from drawable directories
6. npx cap sync android completes without errors after resource changes
</verification>

<success_criteria>
1. Adaptive icon drawables updated with branded "Z" monogram on #1a1a2e — verified by file content grep
2. Splash screen resources updated — old Capacitor logo splash PNGs removed, replacement in place
3. capacitor.config.ts SplashScreen has explicit launchAutoHide: true
4. `npx cap sync android` succeeds after all resource changes
</success_criteria>

<output>
Create `.planning/phases/06-polish-signing-release/06-01-SUMMARY.md` when done
</output>
